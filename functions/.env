# Spark-safe envs (no Secret Manager)
SPARK_ONLY=1

# Webhook auth (REQUIRED)
GMAIL_WEBHOOK_SECRET=PriorityLead2025SecretKey

# Optional – safe to omit on Spark. Add when ready.
OPENAI_API_KEY=sk-REPLACE_ME
