{
  "scripts": {
    "rules:dev:prep": "copy /Y firestore.leads.rules.dev firestore.leads.rules",
    "rules:prod:prep": "copy /Y firestore.leads.rules.prod firestore.leads.rules",
    "deploy:rules:dev": "npm run rules:dev:prep && firebase deploy --project priority-lead-sync --only firestore:rules:leads",
    "deploy:rules:prod": "npm run rules:prod:prep && firebase deploy --project priority-lead-sync --only firestore:rules:leads"
  }
}
