rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /leads_v2/{docId} {
      allow read: if true;      // DEV only. Tighten later for prod.
      allow write: if false;     // Only Cloud Functions should write.
    }
  }
}
